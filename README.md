# AWS Lambda Python Layers Zipper

## Author
Denis Mitrofanov

### Python versions supported:
- 3.6

#### Usage 
```bash
docker run --rm -v $(pwd):/layer denismitr/pylzip opencv-python-headless
```

```bash
docker run --rm -v $(pwd):/layer denismitr/pylzip numpy
```

